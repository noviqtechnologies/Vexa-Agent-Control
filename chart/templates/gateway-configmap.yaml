{{- if and .Values.gateway.enabled (not .Values.gateway.policy.externalConfigMap) -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentcontrol.policyConfigMapName" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "agentcontrol.labels" . | nindent 4 }}
    {{- include "agentcontrol.gatewaySelectorLabels" . | nindent 4 }}
data:
  policy.yaml: |
{{ .Values.gateway.policy.inline | indent 4 }}
{{- end }}
