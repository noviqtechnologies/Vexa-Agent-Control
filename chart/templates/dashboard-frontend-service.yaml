{{- if .Values.dashboardFrontend.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentcontrol.fullname" . }}-dashboard-frontend
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "agentcontrol.labels" . | nindent 4 }}
    {{- include "agentcontrol.dashboardFrontendSelectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboardFrontend.service.type }}
  ports:
  - name: http
    port: {{ .Values.dashboardFrontend.port }}
    targetPort: http
    protocol: TCP
  selector:
    {{- include "agentcontrol.dashboardFrontendSelectorLabels" . | nindent 4 }}
{{- end }}
