{{- if .Values.dashboardDb.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentcontrol.fullname" . }}-dashboard-db
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "agentcontrol.labels" . | nindent 4 }}
    {{- include "agentcontrol.dashboardDbSelectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: postgres
    port: {{ .Values.dashboardDb.port }}
    targetPort: postgres
    protocol: TCP
  selector:
    {{- include "agentcontrol.dashboardDbSelectorLabels" . | nindent 4 }}
{{- end }}
