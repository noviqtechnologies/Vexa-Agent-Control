{{- if .Values.dashboardApi.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentcontrol.fullname" . }}-dashboard-api
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "agentcontrol.labels" . | nindent 4 }}
    {{- include "agentcontrol.dashboardApiSelectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.dashboardApi.port }}
    targetPort: http
    protocol: TCP
  selector:
    {{- include "agentcontrol.dashboardApiSelectorLabels" . | nindent 4 }}
{{- end }}
